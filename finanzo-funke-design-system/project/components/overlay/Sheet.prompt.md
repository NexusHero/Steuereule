Bottom-Sheet für Lücken-Liste, Exporte und Erklärungen; auf Desktop zentriertes Modal.

```jsx
{offen && (
  <Sheet titel="Lücken-Liste (4)" onClose={() => setOffen(false)}>
    …Inhalt…
  </Sheet>
)}
```
