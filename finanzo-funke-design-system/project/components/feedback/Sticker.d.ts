/**
 * Gedrehter Limetten-Sticker für Erfolgsmomente ("+80 €") — Funkes Neufassung des erst-delta aus „Klar".
 */
export interface StickerProps {
  children: React.ReactNode;
  style?: React.CSSProperties;
}
