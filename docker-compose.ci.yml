# CI overlay, layered on docker-compose.yml. Marks the postgres service as running in
# CI mode — grows as real services (API, seed) are added back:
#   docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  postgres:
    environment:
      CI: 'true'
