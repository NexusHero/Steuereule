# CI overlay, layered on docker-compose.yml. For now it just marks the baseline
# container as running in CI — grows as real services are added back:
#   docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  baseline:
    environment:
      CI: 'true'
