# E2E overlay, layered on docker-compose.yml. Marks the postgres service as running in
# E2E mode — grows as real services (API, seed) are added back:
#   docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d
services:
  postgres:
    environment:
      NODE_ENV: development
