# E2E overlay, layered on docker-compose.yml. For now it just marks the baseline
# container as running in E2E mode — grows as real services are added back:
#   docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d
services:
  baseline:
    environment:
      NODE_ENV: development
