/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * SteuerEule API
 * Onboarding profile endpoints (steuereule#29)
 * OpenAPI spec version: 1.0
 */
export interface DeleteAccountRequestDto {
  /** Explicit confirmation — must be exactly `true`. The mandatory pre-delete export offer and Löschschutz warning are a frontend/UX concern; this is the server-side confirmation gate. */
  confirm: true;
  /** Required only when the caller’s current session is not fresh enough (ADR-0013 §6) — re-verified via better-auth’s own password check, never a second hand-rolled one. */
  password?: string;
}

export interface DeletedSummaryDto {
  /** False only when an active LegalHold on "profile" exempted the row — it still exists, retained under legal obligation. */
  profile: boolean;
  /** Always true — the account (User, cascading Session/Account) and its Verification rows are always torn down; LegalHold never exempts login/identity itself. */
  account: true;
}

export interface DeleteAccountResponseDto {
  deleted: DeletedSummaryDto;
  /** Count of TaxDataAccessLog rows anonymised (userId severed to an irreversible tombstone) by this call — retained as the Art. 30 accountability record, no longer person-linkable. */
  retainedAnonymisedAuditRows: number;
  /** Count of rows retained untouched — neither erased nor anonymised — because an active LegalHold exempted them. */
  retainedUnderLegalHold: number;
}

export interface ExportAccountDto {
  email: string;
  name: string;
  emailVerified: boolean;
  createdAt: string;
  authProviders: string[];
}

export interface ExportProfileDto {
  firstName: string;
  lastName: string;
  steuerId: string;
  /** @nullable */
  steuernummer: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface ExportAccessLogEntryDto {
  action: string;
  resource: string;
  createdAt: string;
}

export interface ExportDocumentDto {
  schemaVersion: string;
  exportedAt: string;
  account: ExportAccountDto;
  /** @nullable */
  profile: ExportProfileDto | null;
  /** Always empty until a tax-year model exists. */
  taxData: string[];
  accessLog: ExportAccessLogEntryDto[];
}

export type AccountExportControllerExportAccountParams = {
/**
 * json (default) or pdf.
 */
format?: AccountExportControllerExportAccountFormat;
};

export type AccountExportControllerExportAccountFormat = typeof AccountExportControllerExportAccountFormat[keyof typeof AccountExportControllerExportAccountFormat];


export const AccountExportControllerExportAccountFormat = {
  json: 'json',
  pdf: 'pdf',
} as const;

