/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * SteuerEule API
 * Onboarding profile endpoints (steuereule#29)
 * OpenAPI spec version: 1.0
 */
/**
 * Every stored answer/gate-acknowledgement so far, keyed by question/gate id (packages/core StepId). Empty object for a tax year with nothing answered yet.
 */
export type InterviewStateDtoAnswers = {[key: string]: string};

export type StepDtoKind = typeof StepDtoKind[keyof typeof StepDtoKind];


export const StepDtoKind = {
  question: 'question',
  gate: 'gate',
  done: 'done',
} as const;

export interface StepDto {
  kind: StepDtoKind;
  /** A packages/core StepId (question or gate id). Absent when kind is "done". */
  id?: string;
}

export interface InterviewStateDto {
  /** Every stored answer/gate-acknowledgement so far, keyed by question/gate id (packages/core StepId). Empty object for a tax year with nothing answered yet. */
  answers: InterviewStateDtoAnswers;
  /** @steuereule/core’s nextStep(answers) — never recomputed elsewhere. */
  nextStep: StepDto;
  /** Questions still to answer — @steuereule/core’s remainingSteps(answers). */
  openItems: number;
}

export interface PostAnswerDto {
  /** A question or gate id from the graph (packages/core/src/interview.ts) — validated server-side. */
  questionId: string;
  /** The answer value — validated server-side against this step's own allowed set. */
  value: string;
}

export interface PostAnswerResponseDto {
  /** @steuereule/core’s nextStep(answers) after this write. */
  nextStep: StepDto;
  /** Questions still to answer after this write — @steuereule/core’s remainingSteps(answers). */
  openItems: number;
}

