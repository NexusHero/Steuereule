# Baseline stack. A single idle container that stays alive doing nothing —
# the starting point the rest of the services get built up from.
#   docker compose up -d
services:
  baseline:
    image: alpine:3.20
    command: ['sleep', 'infinity']
    init: true
